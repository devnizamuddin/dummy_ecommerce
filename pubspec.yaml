name: dummy_ecommerce
description: "An example e-commerce project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  # State Management & Dependency Injection
  flutter_bloc: ^9.1.1
  get_it: ^9.2.1

  # Navigation
  go_router: ^17.1.0

  # Networking
  dio: ^5.9.1
  pretty_dio_logger: ^1.4.0

  # Functional Programming
  dartz: ^0.10.1

  # UI Components
  badges: ^3.1.2

  # Local Storage
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
